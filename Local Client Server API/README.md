## Project Setup

To set up the project, run the following commands:

1. Initialize the project and create `package.json`:

   ```bash
   npm init -y
   ```

2. Install Express and generate `node_modules` and `package-lock.json`:

   ```bash
   npm install express
   ```

3. Run the server:

   ```bash
   node server.js
   ```